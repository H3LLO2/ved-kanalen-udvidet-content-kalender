# Google Gemini API Key (required)
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Note: Since we're running locally, the API key will be in the frontend.
# For production, consider using a local proxy server or Edge Functions.
